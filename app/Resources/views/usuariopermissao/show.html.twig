{{ dump(usuarioPermissao) }}

{#
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Usuariopermissao</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuarioPermissao.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('usuariopermissao_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
#}